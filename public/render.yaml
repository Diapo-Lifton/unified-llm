# render.yaml - Render deployment config
services:
  - type: web
    name: unified-llm
    env: node
    region: oregon
    branch: main
    plan: free
    buildCommand: "npm ci"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
